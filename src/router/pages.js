// Imports

const express = require("express");

// Constants

const router = express.Router();
// Exports

module.exports = () => {
    router.get("/", (req, res) => {
        res.render()
    })
};
